# DATABASE CONFIGURATION - Choose one of the following options:

# Option 1 (Default): SQLite database (embedded)
# Leave DATABASE_URL empty or commented out to use SQLite
# Set DATA_DIR to where the SQLite database should be stored
DATA_DIR=./data
# Force SQLite mode by setting an empty DATABASE_URL
DATABASE_URL=

# Option 2: PostgreSQL database (external)
# Uncomment and configure for PostgreSQL connection
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/jellyfin-manager

# Server configuration
NODE_ENV=development
PORT=5000

# Time zone
TZ=UTC

# For Docker: User/Group IDs
PUID=1000
PGID=1000

# Optional: SendGrid API key for email notifications
# SENDGRID_API_KEY=your_sendgrid_api_key